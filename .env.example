# Ninico (Next.js) local env
#
# Points to your Payload backend (Step 20 public delivery endpoints).
# Example: http://localhost:3000
PAYLOAD_API_URL=http://localhost:3000
#
# If you need to call the API from Client Components, Next.js only exposes env vars
# prefixed with NEXT_PUBLIC_. Keep this in sync with PAYLOAD_API_URL.
NEXT_PUBLIC_PAYLOAD_API_URL=http://localhost:3000

